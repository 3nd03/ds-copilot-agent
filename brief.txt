Working directory: capstone/ (scaffold already exists)

GIT RULES, NON-NEGOTIABLE:
- All commits must use the local git config identity already set (mine).
- Never add a "Co-Authored-By" trailer, never mention Claude, Anthropic, or AI
  assistance in any commit message, comment, or file. Commit messages should
  read as if I wrote them: short, plain, describing what changed and why.
- If you cannot commit without adding attribution, stop and tell me instead
  of committing.

Task, in order, stop and log if any step fails after 2 fix attempts:
1. pip install -r requirements.txt
2. Test: python -c "from google.adk.agents import LlmAgent; from google.adk.tools.mcp_tool.mcp_toolset import MCPToolset"
   If this fails, do not guess at fixes for more than 2 attempts. Log the exact
   error in DECISIONS.md and switch to using agents/orchestrator_fallback.py
   instead for the rest of this task.
3. Download the Telco Customer Churn dataset (Kaggle) into data/raw.csv
4. Run mcp_server/data_tools_server.py's tools directly against it (via
   orchestrator_fallback.py's run_pipeline) end to end. Fix any runtime errors
   (dtype issues, column mismatches, SHAP failures). Do not change the core
   logic of profile_data, clean_data, train_models, or explain_model, only
   fix bugs that stop them running.
5. Get streamlit_app.py running locally against the same dataset.
6. Commit working checkpoints as you go, small commits, plain messages.

Log every fix you make in DECISIONS.md: what broke, why, what you changed,
one line each. No other file changes outside what's needed to make the
pipeline run. Do not touch README.md, writeup_blueprint.md, video_script_blueprint.md,
or specs/capstone_spec.md, those are mine to write.

--max-turns 40 --allowedTools "Bash,Read,Edit"